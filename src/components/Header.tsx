import NavBar from "./NavBar";

const Header = () => {
  return (
    <header className="header">
      <div className="navBar">
        <NavBar />
      </div>
    </header>
  );
};

export default Header;
